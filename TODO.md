# TODO — Rekomendasi Animasi untuk Website Portofolio

## Plan yang akan dieksekusi (disetujui user)
1. ✅ Tambahkan elemen target Typing Effect pada `public/index.html` (mis. span dengan data-typing).
2. ✅ Implement typing effect di `public/script.js` (dengan kontrol `prefers-reduced-motion`).
3. Tingkatkan parallax: gerakkan `hero-video-wrap` lebih lambat dari `hero-content` saat scroll (GSAP), desktop + degrade ringan untuk mobile.
4. ✅ Hover micro-interactions untuk `Project Cards` (carousel): zoom halus background, overlay muncul, munculkan judul + tombol saat hover.
5. ✅ Pastikan smooth scrolling saat klik menu navigasi dengan intercept link (menggunakan `scrollIntoView`/GSAP), tetap menghormati mobile.

6. ⏳ Review performa: pastikan animasi tidak berlebihan dan tidak konflik dengan `IntersectionObserver`/ScrollTrigger.


